
/

























Claude · MD
## 統合進捗管理ルール（必須・全セッション共通）

### 作業開始時
セッション開始時、以下のファイルを読み込んでから作業に着手すること。
- ~/Google ドライブ/TERRA_進捗管理/decisions_log.md（過去の教訓と矛盾しないか確認）
- ~/Google ドライブ/TERRA_進捗管理/tech_stack_playbook.md（技術選定判断ルール）
- ~/Google ドライブ/TERRA_進捗管理/projects/<このプロジェクト名>.md（自分自身の現状）

### 作業終了時・大きな変更の完了時
以下を必ず更新すること。

【更新対象1】自プロジェクトのステータスファイル
~/Google ドライブ/TERRA_進捗管理/projects/<このプロジェクト名>.md
- ファイル冒頭の「最終更新」「状態」を更新
- 「進捗」セクションの実装済み/未実装/残タスク/ブロッカーを実態に合わせて書き換える
- 既存の設計情報（基本情報・仕様等のセクション）は、変更がない限り消さずそのまま残す
- 完成度目安(%)は可能な範囲で数値化する

【更新対象2】decisions_log.md への追記（該当する場合のみ）
~/Google ドライブ/TERRA_進捗管理/decisions_log.md
以下に該当する判断・変更があった場合、末尾に追記する（既存の記述は絶対に消さない）。
- 設計変更（特に現場テストの結果によるもの）
- 他プロジェクトにも影響しうる技術判断（オフライン実装方式、UI原則、決済方式等）

フォーマット：
```
### YYYY-MM-DD [プロジェクト名]
- 変更内容：
- 理由・現場での問題点：
- 他プロジェクトへの示唆：
```

### 新規プロダクトに着手する場合
- 必ず decisions_log.md と tech_stack_playbook.md を読み、矛盾する設計をしていないか確認する
- 新しいアイデア段階のものは ~/Google ドライブ/TERRA_進捗管理/idea_inbox.md に記入する（実装着手済みのものは対象外）

### このプロジェクトでの補足（2026-09-22 実地確認）
- **このリポジトリに対応する管理ファイルは `projects/log-truck-volume-app.md`**
  （`terra-kennsyuu-app.md` は存在しない。リポジトリ名と管理ファイル名が違うので注意）
- Claude Code on the web（クラウド実行）のセッションでは、ローカルの
  `~/Google ドライブ/` フォルダは存在しない。**Google ドライブ コネクタ経由なら
  読み書きできることを確認済み**。以下のIDで直接たどれる。

  | ファイル | Drive ファイルID |
  | --- | --- |
  | TERRA_進捗管理（フォルダ） | `1T6WKr0wK1Bxu74PbFeYiie4gcAe0OPJr` |
  | _MASTER_STATUS.md | `16t5I7ncVXFwhDLpHBwvl73DH1705LRWx` |
  | decisions_log.md | `1B58AdtpFMd9LtWFetW1wHOyMHTkR6Xbr` |
  | tech_stack_playbook.md | `1UVAwe0-Hd4TZ80SSkx7rZpihfaQZeSbx` |
  | idea_inbox.md | `1y-3WTlnYZAxU0ZIfno0XwXkYoEuV30lX` |
  | projects（フォルダ） | `1bAaFEHgSYwJDCK7pwlIN66KNzXtXBvE4` |
  | projects/log-truck-volume-app.md | `1XMBMsOUym51jgIop8czQpmUw0dvdDy6K` |

- 読めなかった場合は黙って飛ばさず、その旨を必ず利用者に伝える
- 管理ファイルを更新する際は、**本CLAUDE.mdの記述を正とする**。
  管理ファイル側には方針転換前（Androidネイティブ／Google Play買い切り）の記述や、
  後に誤りと判明したJAS計算式の記述が残っている場合がある

---

TERRA検収PWA（terra-kennsyuu-app）プロジェクト設定
このファイルはClaude Codeがこのリポジトリで作業する際に常に参照する設定です。 新しいセッションや会話が変わっても、ここに書かれた内容は自動的に踏まえて作業してください。

重要：本プロジェクトはAndroidネイティブアプリからPWA（Progressive Web App）へ方針転換しました。以前のバージョンのCLAUDE.mdやコードにAndroid/Kotlin/Gradle関連の記述が残っていても、それらはすべて無効です。ゼロからPWAとして再構築してください。

プロジェクト概要
山林・製材工場での丸太検収作業に特化した、オフラインファーストのPWA（ブラウザで動作するWebアプリ、ホーム画面に追加してネイティブアプリのように使う）。 ターゲットユーザーは林業従事者・素材生産業者・チェンソー技術者。炎天下の屋外・重機騒音下・手袋着用状態で使うことを前提に設計する。UIの説明文を読まない前提の「フールプルーフ設計」を徹底する。

リリースブランド名：TERRA（アプリ名・ロゴ・文言に統一感を持たせる） 配布方式：URLを共有し、ブラウザの「ホーム画面に追加」機能でインストール（アプリストア審査不要） プラットフォーム：スマホブラウザ全般（Android Chrome優先、iOS Safariも可能な範囲で対応）

競合アプリ「Log Counter Plus」の欠陥を解決することが目的：

径級別の小計材積が表示されない
オフライン時にフリーズする
取消操作をすると履歴から完全に消え、何を取り消したか分からなくなる
車番・現場・出荷など不要な情報が常時表示され、画面が煩雑
課金モデル（Android版から変更）
買い切り課金ではなく、年額サブスクリプションとする（他のTERRA系PWAと同じ運用方針に統一）
決済プラットフォームは Lemon Squeezy（ストア開設済み）を使用する
サブスク管理・支払い画面はLemon Squeezyの提供する仕組みに乗せ、アプリ側は「有効なサブスクかどうか」を判定するだけのシンプルな連携にする
オフライン動作が前提のアプリのため、サブスク状態の確認はオンライン時に行い、一定期間（オフライン猶予期間）はローカルに保持した有効期限情報で動作継続できるようにする（現場で通信できない日でも使えなくならないようにするため）
JAS材積計算エンジン（最重要・正確性最優先）
規格長さ L（m、小数点2桁まで）と径級 D（cm）から、以下のロジックで材積を自動計算する。

if L < 6:
    V = D^2 * L / 10000                    # 補正なし。径級によらない
else:  # L >= 6
    Lp = floor(L)                          # L' = 長さの整数部（1mに満たない端数を切り捨て）
    p  = max(0, (Lp - 4) / 2)              # 長さ補正。負になる場合は0
    V  = (D + p)^2 * L / 10000
材積の端数処理は小数第4位を四捨五入する。src/js/jas.js の ROUNDING_MODE 1か所で切り替えられる
（素材の日本農林規格 第6条には径と長さの単位寸法の定めはあるが、材積の端数処理の定めはない。
　工場が丸太材積表を引いて検算したときに数字が合うよう、表と同じ四捨五入に合わせている）
規格長さLは自動判定に使うだけで、計算式の分岐をユーザーに意識させない（内部で自動切替）
径級の刻みはJAS準拠で、14cm未満は1cm刻み、14cm以上は2cm刻み
テストケース：12m材・径級6～72cmの各1本、合計38本で合計92.112m³になることを確認する
JavaScript/TypeScriptでの浮動小数点誤差に注意し、切り捨て処理は文字列操作か整数演算ベースで正確に行うこと

【2026-09-21 修正履歴・重要】
それ以前のこのファイルには、長さ6m以上かつ径級14cm以上のときに径級補正 (D-12)/2 を加える式が
書かれており、「12m材38本で166.698m³になる／92.112m³は競合アプリのバグ」と記載していた。
長野県「林業積算基準 丸太材積表」(3-4_3.pdf) の全1578セル（長さ3.2〜12.8m × 直径4〜56cm）と
照合した結果、この記載が誤りであることが判明した。

  ・上記の正しい式  … 1578セル中 不一致 0件
  ・径級補正を加えた式 … 1578セル中 不一致 1314件
  （例：12m材・末口20cm は材積表では 0.691m³。誤った式では 0.941m³ と約1.36倍になる）

つまり 92.112m³ こそが規格どおりの正しい値であり、バグ値ではない。
この照合は tests/table.test.js が毎回自動実行する。誤った式に戻す変更は必ずテストで落ちる。
なお材積表の値は小数第3位で四捨五入されている。当初は切り捨てにしていたが、表と1mm³ずれる
セルが1578件中683件あったため、ROUNDING_MODE を 'round' にして表に合わせた。

【JAS 第6条から分かった単位寸法（参考・未実装）】
・丸太の径の単位寸法は、小の素材は1cm、その他の素材は2cm。端数は切り捨て
  （本アプリの「14cm未満1cm刻み／14cm以上2cm刻み」と整合している）
・丸太の長さの単位寸法は20cm（0.2m）で端数切り捨て。ただし次の6つは例外的な寸法として扱う
    1.9m / 2.1m / 2.7m / 3.3m / 3.65m / 4.3m
  （丸太材積表の列見出しもこの通りになっている）
  現状アプリは規格長さを自由入力にしているため、2.15m のようなJAS上は存在しない長さも
  受け付ける。これは2026-09-21に検討したうえで「現状維持」と判断済み。
  0.2m刻み＋上記6つへの自動丸めや入力制限は、指示があるまで実装しないこと。
画面構成
画面0：ハンバーガーメニュー（設定・事前登録）
会社名の登録（複数登録可、トラック運送業者は複数得意先、素材生産業者は自社名のみでよい）
トラック車番の登録（複数登録可）
表示言語の選択：日本語（デフォルト）／ベトナム語／タガログ語／タイ語／マレー語
ボタン配置の左右切替（片手持ち最適化）
「よく使う設定」の登録・編集・削除（樹種＋規格長さ＋径級範囲。マニュアル登録方式、学習型自動サジェストは実装しない）
過去の伝票履歴一覧（全件保存、後から参照・再出力可能）
サブスクリプション状態の確認・管理（Lemon Squeezyの顧客ポータルへのリンク）
画面1：セットアップ画面（計測開始前）
「よく使う設定」ショートカットボタンを最上部に表示
樹種：テキストボックスで自由入力
規格長さ（m）：小数点以下2桁まで自由入力（「2」→内部「2.00」表示「2m」、「2.15」→そのまま「2.15m」）
納入規格範囲（径級cm）：2cm刻みの制約があるため「▲▼ボタン」または回転式ピッカーで最小径級・最大径級を選択（自由テキスト入力ではない）
トラック車番／現場名／納入先／自社名：ハンバーガーメニューで事前登録した候補から選択式
備考欄：自由入力、任意項目
「計測開始」ボタン
画面2：開始確認ポップアップ
「（樹種）、（規格長さ）m、納入規格（最小径級）～（最大径級）cm、開始しますか？」を再表示
「はい」ボタンを左側、「戻る」を右側に配置
目的：樹種の切替忘れ防止
画面3：計測入力画面（メイン画面・最重要）
レイアウト最優先事項：

上部のダッシュボードエリア（合計本数・合計材積・入力履歴）は画面全体の高さの3分の2以下に収める
文字サイズは下部の径級ボタンより大きくしない（ボタンを最優先で大きく表示する設計思想）
樹種・材の区分など最小限のみ上部に小さく表示。車番・現場・出荷・日付は画面に一切表示しない（バックグラウンドでは記録）
径級グリッド：

セットアップで指定した範囲の径級のみを動的表示
各カードに「径級」「本数（リアルタイム）」「小計材積（リアルタイム、小数第4位切り捨て）」を横一列に圧縮表示し、カードの縦の高さを抑える
範囲径級数が少ない場合はボタンを自動的に大きく表示する自動リサイズを実装。範囲が広い場合（7～60cm全域など）はスクロール許容
カード全体がタップ領域、ワンタップ＝該当径級の本数+1
タップ時フィードバック：CSS発光アニメーション、Vibration API（対応端末のみ）、Web Audio APIによる操作音の3つすべて実装
誤操作対策：該当カードの長押し（2秒）で-1
入力履歴表示：

上部に直近の入力履歴を一覧表示
取消済みの項目は文字を薄いグレー＋赤の取り消し線で表示し、履歴からは消さず残す（計算からは除外）。ボタン操作・音声入力どちらの取消でも同じ表示ルール
操作ボタン：

「直前取消」ボタン：最後の1件を取消。直下に小さく「長押しで径級ごとの取消もできます」と表示（フールプルーフ設計）
「データ出力」ボタン：セットアップで選択した左右配置に応じて左下または右下に配置
音声入力（補助機能・電波がある時のみ動作前提）：

Web Speech API（ブラウザ標準の音声認識）を使用。これはオンライン前提の機能であり、圏外では動作しなくなる
圏外時は音声入力ボタンをグレーアウトまたは非表示にし、タップ入力への切替を促すメッセージを表示する（フールプルーフ設計。ユーザーが「壊れた」と誤解しないようにするため）
対象範囲内の径級数値のみを認識対象とする（表記揺れの自動統一を含む）
屋外対応：

画面表示中は画面消灯を防止（Screen Wake Lock API）
ダークモードは採用せず、常に明るい背景＋濃い文字色で表示（炎天下での視認性優先）
ボタン・カードの配色は高コントラスト・高彩度を基本方針とするが、初期実装では暫定の色で構わない。CSS変数（カスタムプロパティ）で色を一元管理し、1箇所の変更で全体の配色を差し替えられる構造にしておくこと（実地テスト後に配色調整を行う前提）
画面4：出力確認ポップアップ
樹種・規格長さ・本数・合計材積を再表示
備考欄（自由入力、任意、計測後の追記用）
「本当によろしいですか？」の問いに対し「はい」ボタンを左側、「戻る」を右側に配置
データ出力ボタンとこのポップアップの表示位置・タイミングを分離し、誤操作（連続タップでの誤出力）を防止する
データ出力・共有機能
CSV形式で以下のカラム構成にする：
日付,伝票番号,納入規格(cm),規格長(m),径級(cm),本数,小計材積(m³),累計本数,累計材積(m³),メモ
伝票No.は日付が変わったら自動的に「001」からリセット、同日内は「002」「003」と自動繰り上げ。手入力での上書きも可能にする
「はい」を押すと、Web Share API（navigator.share()）を使ってブラウザ標準の共有ダイアログを表示する。LINE、メール、Googleドライブなど、デバイスにインストールされている共有可能なアプリが自動的にリスト表示される仕組みとし、アプリ側は共有先を個別に実装しない
Web Share APIに対応していないブラウザ環境向けに、CSVファイルの直接ダウンロード（<a download>）をフォールバックとして実装する
CSVヘッダーは選択言語に応じて出力（日本語／ベトナム語／タガログ語／タイ語／マレー語）
データ永続化・バックアップ
ブラウザのIndexedDBに、入力の都度、即座に自動保存する（音声認識・タップいずれの入力も含む。ページ再読み込みやオフライン時のデータ消失を防止）
出力済み伝票は全件IndexedDBに保存し、ハンバーガーメニューの伝票履歴一覧から後から参照・再出力できるようにする
Service Workerによるオフラインキャッシュを実装し、圏外環境でもアプリ本体（HTML/CSS/JS）が正常に読み込める状態にする（オフラインファーストPWAの基本要件）
一定間隔（例：1日1回、Wi-Fi/通信接続時のみ）でIndexedDBのデータをGoogleドライブへ自動バックアップする機能を実装する（スマホの故障・紛失時のデータ消失対策。オフライン思想とは矛盾しない、通信可能な時だけ動く裏処理として実装）
多言語対応
対応言語：日本語（デフォルト）、ベトナム語、タガログ語、タイ語、マレー語
i18nライブラリ（軽量なものを選定、例：i18next等）または自前のJSONベースの辞書方式でUI文言を管理する
ブラウザの言語設定（navigator.language）を自動検出してデフォルト選択、ハンバーガーメニューから手動切替も可能にする
UI全体（ボタン、ラベル、メッセージ、CSVヘッダー）をすべて選択言語で出力する
オフライン同期アーキテクチャ（他のTERRA系PWAで実証済みの設計を踏襲）
基本方針：圏外（現場）での操作は一切ブロックしない。すべてまずローカル（端末）に確定保存し、通信復帰時に自動でクラウドへ送る「オフラインファースト」設計とする。この設計は免税軽油管理PWAで実装・検証済みのパターンであり、本アプリでも同じ考え方を踏襲する。

保存の流れ
計測データ（径級・本数・小計材積等）を入力・確定した瞬間に、テキストデータを即座に端末内IndexedDBへ書き込む。ネット接続の有無に関わらずこの書き込みは常に成功し、UIは即座に「確定完了」を表示する
出力済み伝票（CSV相当のデータ）も同様にIndexedDBへローカル保存する
同期の流れ（通信復帰時、Googleドライブ自動バックアップ機能として実装）
オンライン復帰を検知した時点で自動的に、計測データをGoogleドライブへ同期する
同期状態を管理し、同期待ち（pending）と同期済み（synced）を区別できるようにする
同期失敗時は自動リトライし、長時間（72時間以上を目安）圏外が続いてもデータが消失しない設計とする
信頼性の担保
画面上（ハンバーガーメニューの伝票履歴一覧など）に ☁️（同期待ち）／✅（同期済み）のステータスアイコンを表示する
未同期（pending）のログは、件数に関わらず全件ローカルに保持し続ける（データ消失防止を最優先）
同期済みのログについても、伝票履歴として参照・再出力する機能上、原則削除しない方針とする（丸太検収データは営業証憑としての性質が強く、免税軽油PWAの画像キャッシュのような容量節約目的の自動削除は本アプリでは適用しない）
マスターデータの下り同期
初回セットアップ時に、ハンバーガーメニューで事前登録した会社名・トラック車番・よく使う設定などを一括でIndexedDBへキャッシュしておき、現場での圏外操作を可能にする
実装上の既知の注意点（他PWAでの実例）
Service Workerがクエリパラメータ付きURLをキャッシュせず、オフライン時に特定の画面遷移がフォールバック画面になってしまう問題が、姉妹PWA（免税軽油管理PWA）で確認されている。本アプリでも同様の構成（動的URLパラメータを使う画面遷移）を採用する場合は、Service Workerのキャッシュ戦略でこの点を考慮し、事前に対策を組み込むこと
将来のロードマップ：音声入力のオフライン化（内製モデル訓練）
現時点（初版リリース時）では、音声入力は「電波がある時だけの補助機能」と位置づけているが、将来的には以下のロードマップで、外注ではなく内製でオフライン対応を目指す方針とする。

方針
認識対象は径級の数値（6cm～72cm程度の偶数、約34種類）のみに限定された「キーワードスポッティング」タスクであり、自由文を認識する汎用音声認識に比べて技術的難易度が低い
外部業者への外注ではなく、実際の利用データを蓄積した上でClaude Codeと共同でモデルを訓練する内製ロードマップを採用する。理由：コストを抑えられる、自社独自の学習データとモデルという資産が育つ、現場の訛りや騒音環境に特化した精度向上が期待できる
ロードマップの流れ
データ収集の仕組みを実装：音声入力時の音声データを、利用者の明示的な同意のもとで保存・収集する機能をPWAに組み込む
同意取得フローを必ず実装：「音声データをモデル改善のために保存してよいか」という明確な同意チェックボックスを設ける。同意した利用者のデータのみを収集し、個人が特定できない形で保存する。収集・利用目的はプライバシーポリシーに明記する（技能実習生を含む多国籍の利用者層への配慮を特に重視する）
データ蓄積：各数字につき数百～数千回分の発話サンプルを目安に、実運用の中で自然にデータを蓄積する
モデル訓練：十分なデータが貯まった段階で、Claude Codeと共同でオフライン動作可能な軽量モデル（Vosk、Whisper.cpp等をベースにした限定語彙認識モデル）を訓練する
ブラウザへの組み込み：訓練済みモデルをTensorFlow.jsやONNX等の形式に変換し、PWA内に組み込んで完全オフラインで動作する音声入力を実現する
現時点でのステータス
初版リリース時点ではこのロードマップには着手しない。まずはタップ入力中心のPWAとしてリリースし、実際の利用状況・音声入力へのニーズの強さを見てから、このロードマップへの着手時期を判断する
技術方針
構成：フロントエンドのみのPWA（他のTERRA系PWAと同様、Vercel等へのデプロイを想定）
フレームワークはClaude Codeの判断に任せてよいが、軽量・高速起動・オフライン動作を最優先する（現場でのストレスのない起動速度が重要）
コード変更の際は、まずJAS計算エンジンのテストケースが壊れていないことを確認してから他の変更を進める
配色・文言など仕様に明記のない細部は、実装しやすい合理的な選択を行った上で進めてよい。ただし構造は「1箇所変更で全体反映できる」形にしておくこと（色、多言語文言など）
不明点があれば、現場のプロが炎天下・手袋・騒音下で使うツールであることを常に念頭に置いて判断する
マニフェストファイル（manifest.json）とアイコンを用意し、「ホーム画面に追加」した際にネイティブアプリらしく表示されるようにする

