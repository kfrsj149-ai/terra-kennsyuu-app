# TERRA検収アプリ（terra-kennsyuu-app）プロジェクト設定

このファイルはClaude Codeがこのリポジトリで作業する際に常に参照する設定です。
新しいセッションや会話が変わっても、ここに書かれた内容は自動的に踏まえて作業してください。

## プロジェクト概要

山林・製材工場での丸太検収作業に特化した、完全ローカル完結型のAndroidネイティブアプリ。
ターゲットユーザーは林業従事者・素材生産業者・チェンソー技術者。**炎天下の屋外・重機騒音下・手袋着用状態**で使うことを前提に設計する。UIの説明文を読まない前提の「フールプルーフ設計」を徹底する。

リリースブランド名：TERRA（アプリ名・ロゴ・文言に統一感を持たせる）
価格：¥4,980の完全買い切り（課金・広告要素は一切実装しない）
プラットフォーム：Android ネイティブ、Google Playのみ（iOS対応不要）
完全オフライン動作：通信は一切不要（Googleドライブへのバックアップのみ通信可能時に裏で動作）

競合アプリ「Log Counter Plus」の欠陥を解決することが目的：
- 径級別の小計材積が表示されない
- 長尺材（6m以上）のJAS計算式にバグがあり、径級補正が完全に欠落（過小評価率44.74%）
- オフライン時にフリーズする
- 取消操作をすると履歴から完全に消え、何を取り消したか分からなくなる
- 車番・現場・出荷など不要な情報が常時表示され、画面が煩雑

## JAS材積計算エンジン（最重要・正確性最優先）

規格長さ L（m、小数点2桁まで）と径級 D（cm）から、以下のロジックで材積を自動計算する。

```
if L < 6:
    V = (D + (L - 4) / 2)^2 * L / 10000   # D < 14cm の場合は補正なし、V = D^2 * L / 10000
else:  # L >= 6
    if D >= 14:
        p = (L - 4) / 2 + (D - 12) / 2
        V = (D + p)^2 * L / 10000
    else:
        V = (D + (L - 4) / 2)^2 * L / 10000
```

- 小数第4位以下は**切り捨て**（四捨五入ではない）
- 規格長さLは自動判定に使うだけで、計算式の分岐をユーザーに意識させない（内部で自動切替）
- テストケース：12m材・径級6～72cmの各1本、合計38本で合計166.698m³になることを確認する（短尺式で計算すると誤って92.112m³になる。これは競合アプリのバグと同じ挙動なので、必ず長尺式が正しく適用されることをコード変更のたびに確認する）

## 画面構成

### 画面0：ハンバーガーメニュー（設定・事前登録）
- 会社名の登録（複数登録可、トラック運送業者は複数得意先、素材生産業者は自社名のみでよい）
- トラック車番の登録（複数登録可）
- 表示言語の選択：日本語（デフォルト）／ベトナム語／タガログ語／タイ語／マレー語
- ボタン配置の左右切替（片手持ち最適化）
- 「よく使う設定」の登録・編集・削除（樹種＋規格長さ＋径級範囲。マニュアル登録方式、学習型自動サジェストは実装しない）
- 過去の伝票履歴一覧（全件保存、後から参照・再出力可能）

### 画面1：セットアップ画面（計測開始前）
- 「よく使う設定」ショートカットボタンを最上部に表示
- 樹種：テキストボックスで自由入力
- 規格長さ（m）：小数点以下2桁まで自由入力（「2」→内部「2.00」表示「2m」、「2.15」→そのまま「2.15m」）
- 納入規格範囲（径級cm）：2cm刻みのため「▲▼ボタン」または回転式ピッカーで最小径級・最大径級を選択（自由テキスト入力ではない）
- トラック車番／現場名／納入先／自社名：ハンバーガーメニューで事前登録した候補から選択式
- 備考欄：自由入力、任意項目
- 「計測開始」ボタン

### 画面2：開始確認ポップアップ
- 「（樹種）、（規格長さ）m、納入規格（最小径級）～（最大径級）cm、開始しますか？」を再表示
- 「はい」ボタンを**左側**、「戻る」を右側に配置
- 目的：樹種の切替忘れ防止

### 画面3：計測入力画面（メイン画面・最重要）

**レイアウト最優先事項**：
- 上部ダッシュボード（合計本数・合計材積・入力履歴）は画面全体の高さの**3分の2以下**に収める
- 文字サイズは下部の径級ボタンより大きくしない（ボタンを最優先で大きく表示する設計思想）
- 樹種・材の区分など最小限のみ上部に小さく表示。車番・現場・出荷・日付は画面に一切表示しない（バックグラウンドでは記録）

**径級グリッド**：
- セットアップで指定した範囲の径級のみを動的表示
- 各カードに「径級」「本数（リアルタイム）」「小計材積（リアルタイム、小数第4位切り捨て）」を横一列に圧縮表示し、カードの縦の高さを抑える
- 範囲径級数が少ない場合はボタンを自動的に大きく表示する自動リサイズを実装。範囲が広い場合（7～60cm全域など）はスクロール許容
- カード全体がタップ領域、ワンタップ＝該当径級の本数+1
- タップ時フィードバック：発光・振動（Haptic Feedback）・操作音の3つすべて実装
- 誤操作対策：該当カードの長押し（2秒）で-1

**入力履歴表示**：
- 上部に直近の入力履歴を一覧表示
- 取消済みの項目は文字を薄いグレー＋赤の取り消し線で表示し、履歴からは消さず残す（計算からは除外）。ボタン操作・音声入力どちらの取消でも同じ表示ルール

**操作ボタン**：
- 「直前取消」ボタン：最後の1件を取消。直下に小さく「長押しで径級ごとの取消もできます」と表示（フールプルーフ設計）
- 「データ出力」ボタン：セットアップで選択した左右配置に応じて左下または右下に配置

**音声入力**：
- フローティングボタン、オンデバイス音声認識（Android標準SpeechRecognizer、オフライン優先）
- 対象範囲内の径級数値のみ認識対象（表記揺れの自動統一を含む）
- 30～50分の連続作業でも認識が止まらないよう自動再開ループを実装

**屋外対応**：
- 画面表示中は画面消灯を防止（Keep Screen On）
- アプリから画面の明るさを最大に設定する機能
- ダークモードは採用せず、常に明るい背景＋濃い文字色（炎天下での視認性優先）
- ボタン・カードの配色は高コントラスト・高彩度が最終目標だが、初期実装は暫定色でよい。colors.xml等、1箇所の変更で全体の配色を差し替えられる構造にしておく（実地テスト後に調整前提）

### 画面4：出力確認ポップアップ
- 樹種・規格長さ・本数・合計材積を再表示
- 備考欄（自由入力、任意、計測後の追記用。セットアップ画面の備考とは別項目）
- 「本当によろしいですか？」に対し「はい」ボタンを**左側**、「戻る」を右側に配置
- データ出力ボタンとこのポップアップの表示位置・タイミングを分離し、誤操作（連続タップでの誤出力）を防止する

## データ出力・共有機能

CSVカラム構成：
```
日付,伝票番号,納入規格(cm),規格長(m),径級(cm),本数,小計材積(m³),累計本数,累計材積(m³),メモ
```

- 伝票No.は日付が変わったら自動的に「001」からリセット、同日内は自動繰り上げ。手入力での上書きも可能にする
- 「はい」を押すとAndroid標準の `Intent.ACTION_SEND` によるOSネイティブ共有ダイアログを表示。LINE、メール（Gmail起動）、Googleドライブ、メモ帳など、デバイスにインストールされている共有可能なアプリが自動リスト表示される仕組みとし、共有先は個別実装しない
- CSVヘッダーは選択言語に応じて出力

## データ永続化・バックアップ

- ローカルDB（SQLite）に、入力の都度、即座に自動保存する（音声・タップいずれも含む。強制終了時のデータ消失を防止）
- 出力済み伝票は全件ローカルDBに保存し、ハンバーガーメニューの伝票履歴一覧から後から参照・再出力できるようにする
- 一定間隔（例：1日1回、Wi-Fi接続時のみ）でローカルDBをGoogleドライブへ自動バックアップする機能を実装する

## 多言語対応

- 対応言語：日本語（デフォルト）、ベトナム語、タガログ語、タイ語、マレー語
- Android標準のリソースファイル方式（`values-vi`、`values-tl`、`values-th`、`values-ms`等）でstrings.xmlを分離管理
- デバイスのシステム言語を自動検出してデフォルト選択、ハンバーガーメニューから手動切替も可能
- UI全体（ボタン、ラベル、メッセージ、CSVヘッダー）をすべて選択言語で出力

## 技術方針

- 開発言語：Kotlin推奨だが実装のしやすさを優先してよい
- コード変更の際は、まずJAS計算エンジンのテストケースが壊れていないことを確認してから他の変更を進める
- 配色・文言など仕様に明記のない細部は、実装しやすい合理的な選択を行った上で進めてよい。ただし構造は「1箇所変更で全体反映できる」形にしておくこと（色、多言語文言など）
- 不明点があれば、現場のプロが炎天下・手袋・騒音下で使うツールであることを常に念頭に置いて判断する
