---
最終更新: 2026-09-22
プロジェクト名: 丸太検尺(材積計算)アプリ / LOG-RA
状態: 実装完了・mainへマージ済み。Vercelへのインポート待ち
---

> このファイルはクラウドモード（Claude Code on the web）で作成した進捗記録です。
> Google ドライブの `TERRA_進捗管理/projects/log-truck-volume-app.md` と同じ役割を持ちます。
> Drive側への反映は、チャット(claude.ai)側での同期作業が別途必要です。

## 位置づけ
- 目的: 山林圏外・重機騒音下で「文字入力ゼロ・1秒記録」を実現するオフラインファーストの検収アプリ
- ターゲット: 全国の素材生産業者、個人事業主、森林組合、市売市場の検収員
- TERRA全体への位置づけ: 「第一の現金化ポイント」。単体販売で即時キャッシュフロー獲得 → SHIN-RA等本体開発の軍資金化

## 基本情報
- 技術スタック: **PWA**（素のHTML / CSS / ESモジュール。ビルド不要、依存パッケージゼロ）
  - 2026-09-20にAndroidネイティブ（Kotlin+SQLite）から方針転換
  - 保存は IndexedDB、オフライン動作は Service Worker
  - ホスティングは Vercel（GitHub連携で自動デプロイ）
- オフライン対応: 必須（100%圏外動作保証、共有ダイアログ表示まで含む）
- 決済システム: **Lemon Squeezy 年額サブスクリプション**（Google Play買い切りから変更）
  - オフライン猶予30日、初回起動から14日間の試用期間つき
- 価格: 年額サブスク（金額は未確定）
- リポジトリ: kfrsj149-ai/terra-kennsyuu-app

## 競合分析（2026-08-27 実検証 → 2026-09-21 重要な訂正）
競合アプリ「Log Counter Plus」の欠陥：
- 径級別小計の不表示（全体合計m³のみ、現場で二度手間）
- オフライン時のフリーズ（広告モデル・オンライン前提設計）
- 取消操作をすると履歴から完全に消え、何を取り消したか分からなくなる
- 車番・現場・出荷など不要な情報が常時表示され、画面が煩雑

**【訂正】長尺材の計算バグという指摘は、こちら側の誤りだった**
- 旧記載:「12m材38本で92.112m³(誤) vs 166.698m³(正)、径級補正(D-12)/2が完全欠落、
  過小評価率44.74%、単発損失約74.6万円相当、市場に数千万円規模の損害」
- 実際: 長野県「林業積算基準 丸太材積表」(3-4_3.pdf) の**全1578セル**
  （長さ3.2〜12.8m × 直径4〜56cm）と照合した結果、
  **92.112m³ が規格どおりの正しい値**であり、166.698m³ のほうが誤りだった
  - 正しい式（径級補正なし）… 1578セル中 不一致 **0件**
  - 径級補正(D-12)/2を加えた式 … 1578セル中 不一致 **1314件**
  - 例: 12m材・末口20cm は材積表では 0.691m³。誤った式では 0.941m³（約1.36倍）
- **径級補正 (D-12)/2 は素材の日本農林規格に存在しない**
- 営業資料・販売ページで「競合の計算バグ」を訴求してはならない

## 核心機能・仕様
- 径級ごとの本数＋小計材積をリアルタイム表示
- JAS規格準拠（素材の日本農林規格・末口二乗法）
  - L < 6m : V = D² × L ÷ 10000（補正なし。径級によらない）
  - L >= 6m: V = (D + (L′− 4)/2)² × L ÷ 10000　L′= 長さの整数部
  - 端数処理は小数第4位を四捨五入（JASに材積の端数処理の定めがないため、
    工場が引く丸太材積表と同じ丸め方に合わせた）
  - 径級の刻みは14cm未満が1cm、14cm以上が2cm
  - 計算は浮動小数点を一切使わず BigInt（整数）のみ。1万本積んでも誤差ゼロ
- 大ボタンワンタップUI（手袋・リフト操作中でも操作可能）
- CSV出力・ローカル保存（Web Share APIでLINE等へ共有、非対応環境はダウンロード）
- 音声入力（Web Speech API。オンライン時のみの補助機能。圏外では自動的に無効化）
- データ消失ゼロ設計（入力の瞬間にIndexedDBへ即時保存）

## UIフロー（確定事項）
- 画面0 ハンバーガーメニュー: 会社名・トラック車番・現場名・納入先の事前登録、
  言語切替（5言語）、ボタン左右切替、よく使う設定、伝票履歴、サブスク、バックアップ
- 画面1 セットアップ: 規格長さ(小数2桁自由入力)、納入規格範囲(▲▼ピッカー)、樹種は自由入力
- 画面2 開始確認ポップアップ: 樹種・規格長・納入規格範囲を再表示（樹種の切替忘れ防止）
- 画面3 計測入力: 上部ダッシュボード（合計本数・合計材積・入力履歴1行）は画面の約15〜25%。
  径級ボタンは**2列グリッド**。タップで+1、長押し2秒で操作が分岐する
  - 本数の数字を長押し → その径級の本数を直接入力（長押し中は枠が青）
  - それ以外を長押し → −1（長押し中は枠が赤）
  - 入力ボタンには材積を表示しない（径級と本数のみ、文字を大きく）
  - 入力履歴は**1行のみ**。古いものは左へ流れ、最新が常に右端。区切りは半角カンマ+スペース
  - 取消は履歴から消さず、グレー＋赤の取消線で残す（計算からは除外）
- 画面4 出力確認ポップアップ: 伝票番号・日付を最上部に置き、
  **径級別の明細表（径級／単材積／本数／小計材積）＋合計**を表示。
  工場の手書き伝票への転記元として使う。確認文とボタンはスクロールの外に固定
  - 単材積は小数4桁表示。3桁だと「単材積×本数=小計」の手計算が合わないため
- 出力: Web Share API によるOS標準の共有ダイアログ
- 多言語対応: 日本語(デフォルト)/ベトナム語/タガログ語/タイ語/マレーシア語。CSVヘッダーも選択言語
- 屋外対応: Screen Wake Lock APIで画面消灯防止、ダークモードではなく明背景+濃文字、
  片手持ち最適化（ボタン配置を左右選択可）、配色はCSS変数で一元管理
- 改善5項目（全採用・実装済）: ①伝票履歴を全件保存し後から参照・再出力 ②タップ時フィードバック
  (発光・振動・音) ③伝票No.自動採番(日替わりで001からリセット) ④取消の視覚化 ⑤ショートカット登録

## 価格・収益モデル
- 年額サブスクリプション（Lemon Squeezy、Merchant of Recordのため消費税・VAT申告を代行）
- 販売チャネル: URL配布＋「ホーム画面に追加」（アプリストア審査不要）。
  岩手・秋田・山形・新潟等の林業密集地域を最優先ターゲット
- 獲得資金はTERRA本体開発（SHIN-RA、ZOU-RA等）の軍資金に充当

## スケジュール
- 目標リリース時期: 2026年10月末〜11月末（秋冬の検収シーズン前）

## 進捗
- **完成度目安: 約85%**（実装は完了。公開作業と現場テストが残り）
- 2026-09-22 時点の実地確認:
  - **PR #1 はマージ済み**。`origin/main` にアプリ一式が入り、未反映コミットは0件
    （マージコミット a685e87）
  - Vercel側に `terra-kennsyuu-app` プロジェクトは**未作成**。
    チーム `menzei-pwa` には field-pwa / menzei-keiyu-pwa / office-pwa /
    phase0-prototype の4件のみ（Vercelコネクタで確認）
  - 自動テスト19件はすべて合格
- 実装済み:
  - JAS材積計算エンジン（BigInt整数演算、公的材積表1578セルと全件一致）
  - 全画面（メニュー／セットアップ／開始確認／計測入力／出力確認／伝票履歴）
  - IndexedDBへの即時保存、下書き復帰、伝票の全件保存と再出力
  - Service Workerによるオフライン動作（クエリパラメータ付きURLでも起動する）
  - CSV出力＋Web Share API（フォールバックのダウンロードも）
  - 5言語対応（UI文言・CSVヘッダー）
  - 音声入力（Web Speech API、圏外で自動無効化）
  - 発光／振動／操作音、画面消灯防止
  - Lemon Squeezyライセンス判定（試用14日、オフライン猶予30日）
  - Googleドライブ自動バックアップ（clientId未設定のため現在は無効表示）
  - 自動テスト19件（材積表の全セル照合を含む）
  - PWAアイコン、マニフェスト、Vercel設定
  - プライバシーポリシー（privacy.html、圏外でも閲覧可。事業者情報は未記入）
- 未実装・作業中:
  - **プライバシーポリシーの事業者情報・問い合わせ窓口の記入**（privacy.html）
  - Lemon Squeezyの商品URL・顧客ポータルURLの設定（src/js/config.js）
  - GoogleドライブOAuthクライアントIDの設定（src/js/config.js）
  - 音声入力の実機での精度確認（ヘッドレス環境では検証不可）
  - 配色の実地調整（現在は暫定色。tokens.cssの1か所で変更可能）
- ローンチまでの残タスク（優先順）:
  1. **PR #1 をmainへマージする**（現在 open のまま。mainにはCLAUDE.mdしかない）
  2. Vercelへインポートして公開（https://vercel.com/new から。vercel.json設定済みのため設定変更不要）
  3. スマホで「ホーム画面に追加」→ 圏外テスト・LINE共有テスト
  4. config.js にLemon SqueezyのURLとGoogle DriveのclientIdを設定
  5. 現場での実地テスト（炎天下の視認性、手袋での操作性、配色調整）
  6. 価格決定と、プライバシーポリシーへの事業者情報の記入
- ブロッカー:
  - ~~PR #1 が未マージ~~ → **2026-09-22 解消**
  - **Vercelへのインポートが未完了**。利用者が手作業で行う必要がある。
    https://vercel.com/new を開き `terra-kennsyuu-app` を Import → そのまま Deploy
    （`vercel.json` に設定済みのため設定変更は不要）
  - 一覧に出ない場合は、GitHubアプリのリポジトリ許可が「一部のみ」になっている
    可能性がある（Adjust GitHub App Permissions から `terra-kennsyuu-app` を追加）
  - VercelのMCPコネクタからは**チーム `menzei-pwa` への書き込みができない**
    （プロジェクト一覧の取得はできるが、作成は 403 forbidden。
    `You must re-authenticate to this scope` と返る）。
    Claude側から自動でデプロイしたい場合は、このスコープへの再認証が必要

## ローンチ後の実績
- 利用者数: -
- 売上: -

## 他プロジェクトへの示唆
- 競合の欠陥分析手法、JAS計算式ロジック、オフライン入力の技術要件は他の現場向けアプリにも転用可能
- **公的な一次資料（材積表等）との全件照合をテストに組み込む手法**は、
  計算が business critical な他プロジェクトにそのまま使える

## decisions_log転記用

### 2026-09-24 [log-truck-volume-app]
- 変更内容：収集していない音声データについて同意を求めるUIを取り下げ、
  config.js のフラグ（初版は false）で出し分けるようにした。
  あわせてプライバシーポリシー(privacy.html)を追加し、圏外でも読めるようにした。
- 理由・現場での問題点：「同意した場合のみ音声が保存されます」と表示しながら、
  録音・保存のコードが一行も存在しなかった。チェックを入れても何も起きないのに、
  利用者には「音声を集めている」と受け取られる。技能実習生を含む多国籍の利用者に
  意味の分からない同意を求めることになり、誠実さの面でも問題がある。
  また、Web Speech API はブラウザ提供元のサーバーへ音声を送って認識するため、
  「本アプリは音声を保存しない」と「音声が端末外へ出る」は両立する。
  この事実を利用者が知らないまま使うことのほうがリスクが大きい。
- 他プロジェクトへの示唆：**同意UIは、実際に収集を始めるのと同時に出すこと。
  先回りして置くと空手形になる。**また、ブラウザ標準APIを使う機能は、
  その API 自体が外部へデータを送っていないかを確認し、プライバシーポリシーに
  書くこと（Web Speech API、翻訳API、フォント配信などが該当しうる）。

### 2026-09-22 [log-truck-volume-app]
- 変更内容：統合進捗管理ルールにクラウドモードを追加した。作業開始時に
  `~/Google ドライブ/TERRA_進捗管理/` の有無を判定し、無ければリポジトリ直下の
  STATUS_LOCAL.md に同じフォーマットで記録する運用にした。
- 理由・現場での問題点：Claude Code on the web（クラウド実行）では
  ローカルのGoogle ドライブ同期フォルダが存在せず、進捗管理ルールが
  そのままでは実行できなかった。気づかずに飛ばされると進捗記録が欠落する。
- 他プロジェクトへの示唆：**ローカル前提のパスを含む運用ルールは、
  クラウド実行環境でのフォールバック先を必ず併記すること。**
  他のTERRA系プロジェクトのCLAUDE.mdにも同じクラウドモードの記述を入れる必要がある。
  なおGoogle ドライブ コネクタ経由なら読み書き自体は可能だが、
  運用を単純に保つため既定はリポジトリ内ファイルとした。

### 2026-09-21 [log-truck-volume-app]
- 変更内容：JAS長尺材の計算式から径級補正 (D-12)/2 を削除し、規格どおりの式に修正した。
  あわせて、競合アプリ「Log Counter Plus」に長尺材の計算バグがあるという分析を撤回した。
  公的な丸太材積表の全1578セルとの照合テストを自動テストに組み込んだ。
- 理由・現場での問題点：長野県「林業積算基準 丸太材積表」と突き合わせたところ、
  こちらの式は1578セル中1314件が不一致だった（正しい式は不一致0件）。
  12m材・末口20cmが材積表では0.691m³のところ0.941m³と約1.36倍になっていた。
  「競合の過小評価率44.74%・数千万円規模の損害」という分析は、こちらの計算誤りが原因だった。
- 他プロジェクトへの示唆：**計算ロジックが business critical なプロダクトでは、
  仕様書のテストケースを信用せず、公的な一次資料と全件照合するテストを必ず用意すること。**
  仕様書に書かれた期待値そのものが誤っている場合があり、テストが通っていても意味がない。
  また、競合の欠陥を営業訴求に使う前に、自社側の計算を一次資料で検証すること。

### 2026-09-20 [log-truck-volume-app]
- 変更内容：AndroidネイティブアプリからPWAへ全面的に方針転換した。
  フレームワークを使わず、素のHTML/CSS/ESモジュールのみのビルド不要構成を採用。
- 理由・現場での問題点：Google Playの審査と手数料15-30%を回避したいこと、
  URL共有だけで配布できること、現場での起動速度を最優先したいこと。
  ビルド工程をなくしたことで、依存パッケージの更新やビルド環境の維持が不要になった。
- 他プロジェクトへの示唆：**OS深部機能（カメラ・センサー・バックグラウンド処理）を
  使わない現場向けツールは、PWAで十分に要件を満たせる。**
  オフライン動作はService Worker + IndexedDBで実用レベルに達する。
  tech_stack_playbook.md の判定フローQ2「PWAのService Worker/IndexedDBでは限界がある」は、
  この規模のアプリには当てはまらなかった。

### 2026-09-20 [log-truck-volume-app]
- 変更内容：PWAの決済をStripeではなくLemon Squeezyの年額サブスクリプションにした。
- 理由・現場での問題点：Lemon SqueezyはMerchant of Recordであり、消費税・VATの
  申告と納付を代行してくれる。個人事業主が海外含めて販売する際の事務負担が大きく変わる。
  ライセンスキーの検証APIがブラウザから直接叩けるため、アプリ側の実装も単純になる。
- 他プロジェクトへの示唆：**tech_stack_playbook.md の決済原則「PWA → Stripe」は
  Lemon Squeezyも選択肢に加えて改訂すべき。**手数料はStripeより高いが、
  税務処理の代行分を考えると個人・小規模事業者にはLemon Squeezyが有利な場合が多い。

### 2026-09-21 [log-truck-volume-app]
- 変更内容：Vercelの `cleanUrls: true` を使わないことにした。
  あわせてService Worker側で、リダイレクト経由で取得した応答を素の応答に作り直してから
  キャッシュするようにした。
- 理由・現場での問題点：cleanUrlsが有効だと `/index.html` が別URLへ308リダイレクトされ、
  リダイレクト経由の応答はキャッシュから配信できないため、圏外でアプリが真っ白になる。
  プリキャッシュ自体は成功して「24件キャッシュ済み」と表示されるので、極めて気づきにくい。
  Vercelの配信を模したサーバーで再現して確認した。
- 他プロジェクトへの示唆：**オフライン必須のPWAでは cleanUrls / trailingSlash 等の
  URL書き換え設定を有効にしてはいけない。**姉妹PWA（免税軽油管理PWA）で報告されていた
  「クエリパラメータ付きURLがオフラインでフォールバックする」問題と同じ根を持つ。
  Service Worker側でリダイレクト応答を正規化しておけば二重に防げる。
